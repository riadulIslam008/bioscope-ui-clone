import 'package:flutter/material.dart';

class MoviesCollection extends StatelessWidget {
  const MoviesCollection({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}