import 'package:flutter/material.dart';

class DramaCollections extends StatelessWidget {
  const DramaCollections({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}